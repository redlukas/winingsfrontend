#!/bin/bash

BACKEND_IP=ipgoeshere
BACKEND_PORT=portgoeshere

##this script should be run in a 32bit respbian install on startup
##it assumes the rasPiEnvironmentSetup has already run

##register the environment variables

##if necessairy, rebuild the frontend prod build

##start the backend server

##start the frontend server
